import SitePage from "../components/SitePage";

export default function FamiliesPage() {
  return (
    <SitePage
      eyebrow="Protein families"
      title="Explore cuticular protein families"
      description="Browse curated cuticular protein families, conserved sequence features, HMM-supported classifications, and associated references."
    />
  );
}

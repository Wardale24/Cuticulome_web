import SitePage from "../components/SitePage";

export default function ContactPage() {
  return (
    <SitePage
      eyebrow="Contact"
      title="Contact the Cuticulome.db team"
      description="Get in touch regarding database contributions, large protein submissions, data corrections, collaborations, or questions about Cuticulome.db."
    />
  );
}

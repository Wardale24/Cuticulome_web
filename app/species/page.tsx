import SitePage from "../components/SitePage";

export default function SpeciesPage() {
  return (
    <SitePage
      eyebrow="Species"
      title="Browse species"
      description="View species represented in Cuticulome.db, inspect available cuticular protein sets, and access species-specific downloads."
    />
  );
}

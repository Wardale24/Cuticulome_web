import SitePage from "../../components/SitePage";

export default function ClassifierPage() {
  return (
    <SitePage
      eyebrow="Protein classification"
      title="Cuticular Classifier"
      description="Classify candidate proteins into cuticular protein families using curated HMM models and sequence feature evidence."
    />
  );
}

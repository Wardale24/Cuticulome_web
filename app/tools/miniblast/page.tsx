import SitePage from "../../components/SitePage";

export default function MiniBlastPage() {
  return (
    <SitePage
      eyebrow="Sequence search"
      title="miniBLAST"
      description="Compare a protein sequence against all curated proteins in Cuticulome.db using a lightweight BLAST-style search tool."
    />
  );
}

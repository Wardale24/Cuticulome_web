import SitePage from "../components/SitePage";

export default function DownloadsPage() {
  return (
    <SitePage
      eyebrow="Downloads"
      title="Download curated datasets"
      description="Access FASTA files, species-specific protein sets, family-specific datasets, and reference tables from Cuticulome.db."
    />
  );
}
